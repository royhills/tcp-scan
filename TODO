$Id$

Don't display closed ports by default [need to check engine usage].

Change --data (-D) option to something more sensible [need to check engine usage].

Display port names by default [need to check engine usage].

Review default bandwidth, and possibly increase it.

Add better "make check" tests and measure code coverage.

Allow the use of networks and ranges for the target hosts.

Add IPv6 support

Complete manpage tcp-scan.1

Add option to use different services file.

Remove references to RMIF environment variable.

Only display brief help message for usage errors.
